# HCLM-D Dashboard — z86.dev
# Copy to .env and fill in values

# Server
DASHBOARD_PORT=3000
DASHBOARD_DB=./metrics.db

# Cloudflare (optional — for automated DNS management)
CF_API_TOKEN=
CF_ZONE_ID=

# CORS (comma-separated origins allowed)
CORS_ORIGINS=https://z86.dev,http://localhost:5173
